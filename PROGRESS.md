# Состояние работы

**Фаза «кокпит по сети»: планы 1 и 2 из 4 сделаны.**
План 1 смержен (PR #23). План 2 — ветка `feat-tray-handoff`, 959 тестов,
**ждёт живой приёмки пользователем** (клик по значку в трее и галочка автозапуска —
единственное, что нельзя проверить программно).

## Что работает

Кокпит отдаёт свой интерфейс по сети. Браузер на другой машине **подключается** к
живым вкладкам (не создаёт новые), видит историю вывода, печатает в терминал,
видит агентов и слэш-команды.

План 2 добавил к этому:

- **Трей.** Крестик прячет окно, а не завершает кокпит; выход — только пунктом меню.
  В меню видно, где сейчас управление, и адрес для браузера. Иконка приглушается,
  когда рулит другая машина.
- **Автозапуск** при входе в Windows (галочка в меню трея, по умолчанию выключена).
  Окно при этом не показывается — кокпит поднимается сразу в трей и восстанавливает
  вкладки.
- **Эстафета управления.** Один хозяин в каждый момент: открыл страницу в браузере —
  забрал руль, окно ПК спряталось; показал окно из трея — забрал обратно. Невладелец
  видит заглушку и может смотреть (статусы, лимиты, дашборд, поиск истории), но не
  писать. Размер терминала передаётся при пересадке — «плывущий масштаб» лечится этим.
- **Обрыв связи виден и лечится сам**: «Нет связи с кокпитом», переподключение с
  нарастающим интервалом, затем перезагрузка страницы. Сессии на ПК не трогаются.

Ключевые файлы плана 2: `src/main/{ownership,write-channels,tray-menu,autostart}.js`,
проводка — `createHandoffWiring` в `ipc.js`, интерфейс — `src/renderer/js/handoff-view.js`
и обвязка в `app.js`/`api-boot.js`.

## Удалённый доступ ВКЛЮЧЁН

В `%APPDATA%\claude-cockpit\config.user.json`:

```json
"net": {
  "host": "100.120.245.85",
  "port": 48300,
  "allowedHosts": ["revision-pc.tailb86363.ts.net", "[fd7a:115c:a1e0::a136:f557]"]
}
```

Адреса: `http://100.120.245.85:48300` и `http://revision-pc.tailb86363.ts.net:48300`.

**Аутентификации НЕТ** — она в плане 4. Любое устройство в тайлнете (включая телефон,
который можно потерять) получает командную строку на этой машине. Осознанный
компромисс владельца.

## Чек-лист приёмки плана 2

1. Крестик на окне → окно спряталось, кокпит жив (значок в трее), уведомление один раз.
2. Клик по значку в трее → окно вернулось.
3. Открыть `http://100.120.245.85:48300` с макбука → окно на ПК спряталось само,
   с макбука можно печатать.
4. Показать окно на ПК → на макбуке легла заглушка с кнопкой «Забрать управление себе».
5. Пока рулит макбук — попробовать напечатать на ПК: ничего не происходит, ошибок нет.
6. Закрыть крышку макбука → окно ПК само не выпрыгивает, вкладки живы.
7. Меню трея → «Автозапуск»: галочка встаёт и **снимается** (проверялось через реестр).
8. Меню трея → «Выход»: процесс завершился, порт освободился.

## Известные ограничения

- Вкладки, открытые другим клиентом, не приезжают сами — подхватываются при получении
  управления (события `tabs:changed` в форме api нет).
- `Ctrl+Tab` в браузере недоступен, переключение вкладок — по номеру.
- `Cmd+W` закроет вкладку с кокпитом, перехватить нельзя.
- Спящий ПК недоступен: Tailscale его не разбудит.

## Следующее: планы 3 и 4

Файловый обзор для выбора директории (главное требование: новые сессии из любой
папки с любой машины); токен и HTTPS через Tailscale Serve.

- Спека: `docs/superpowers/specs/2026-08-04-cockpit-remote-design.md`
- План 1 (закрыт): `docs/superpowers/plans/2026-08-04-cockpit-remote-foundation.md`
- План 2 (закрыт): `docs/superpowers/plans/2026-08-05-cockpit-tray-handoff.md`

## Разработка ведётся в дубликате

`C:\Users\Lunev\AssistClaude\claude-cockpit-net` — копия на том же репозитории.
**Запускать только с флагом изоляции**, иначе перезапишет состояние рабочего кокпита
(имя приложения общее, значит и папка хранилища):

```
npm start -- --user-data-dir=C:\Users\Lunev\AppData\Roaming\cockpit-net-data
npm run smoke -- --user-data-dir=C:\Users\Lunev\AppData\Roaming\cockpit-net-data
```

Пользовательский конфиг в этом каталоге называется **`config.user.json`** (не
`config.json`) — в нём же задаётся свой порт, чтобы не драться с рабочим кокпитом.

## Помнить

- Bash на этой машине съедает бэкслэши в путях — команды с путями через PowerShell.
- `Select-Object -First N` в конвейере со `npm run smoke` обрывает поток и роняет
  процесс: код выхода получается ненулевым при полностью успешном прогоне.
- Ревьюеру, проверяющему тесты мутациями, работать в изолированной копии дерева
  (`git worktree` + junction на `node_modules`), а не в рабочем.
- Параллельным агентам — только непересекающиеся файлы; `git add` поимённо, не `-A`.
- Двойные кавычки и длинное тире в `git commit -m` ломают разбор командной строки
  PowerShell — сообщение писать файлом и `git commit -F`.
- Урок трёх кругов ревью плана 2: 6 Critical, из них **четыре жили в исправлениях
  предыдущего круга**, а не в исходной работе. Ни один не был виден в дифе — все
  найдены запуском: браузером, убийством процесса, чтением реестра, мутациями.
